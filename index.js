require('react-devtools-core').connectToDevTools({host: '192.168.1.105'})
import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('ViewAnimation', () => App);
